version: "3.3"

services:
  postgres:
    image: postgres:15
    environment:
      - POSTGRES_DB=cantera_music
      - POSTGRES_USER=abril
      - POSTGRES_PASSWORD=ob4dOyaRnj7eOVNZzRd50YrQLbOkboP1
    ports:
      - 5432:5432
    volumes:
      - ./postgres_data:/var/lib/postgresql/data

  pgadmin:
    image: dpage/pgadmin4
    environment:
      - PGADMIN_DEFAULT_EMAIL=admin@mail.com
      - PGADMIN_DEFAULT_PASSWORD=root
    ports:
      - 5050:80
